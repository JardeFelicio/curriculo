import 'package:flutter/material.dart';

class ResumoScreen extends StatelessWidget {
  static String routeName = '/resumoscreen';
  const ResumoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
